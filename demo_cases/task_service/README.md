# Task Service Demo Case

This case represents a minimal backend business module:

- list tasks
- create tasks
- mark tasks as complete

It is paired with `specs/task_service/` and is intended to exercise:

- structured specification loading
- test-first contract generation
- state-machine orchestration
- graph sync after implementation
