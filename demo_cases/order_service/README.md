# Order Service Demo Case

This case extends the prototype beyond CRUD-only behavior:

- create an order
- retrieve order details
- update order status with business-rule constraints

It is paired with `specs/order_service/` and is intended to support future baseline and repair experiments.
